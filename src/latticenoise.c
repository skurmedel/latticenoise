// MATH FUNCTIONS.

/*
	Performs linear interpolation between a and b using ratio r.

	a & b:

		The values to interpolate between.

	r:

		The ratio or position between a & b that is desired. Only a value in the 
		range [0.0, 1.0) is allowed.

	Returns:

		The interpolated value on success.
		NaN on error.
*/
static float lerp(float a, float b, float r)
{
	
}

